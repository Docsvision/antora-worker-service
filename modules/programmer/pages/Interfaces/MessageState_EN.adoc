= MessageState -- перечисление

Состояние сообщения.

* *Пространство имён:* `Docsvision.WorkerService.Interfaces`
* *Сборка:* `DocsVision.WorkerService.ObjectModel.dll`

== Синтаксис

[source,csharp]
----
public enum MessageState
----

[cols=",",options="header"]
|===
|Имя члена |Описание

|`Unhandled = 0`
|_Не обработано_ -- сообщение ещё не обрабатывалось.

|`Handled = 1`
|_Обработано_ -- сообщение было обработано.

|`Error = 2`
|_Ошибка_ -- обработка сообщения завершилась с ошибкой.

|`Failed = 3`
|Неудачно

|`Paused = 4`
|_Блокировано_ -- при обработке сообщения возникла ошибка, но обработка не завершена.

|===
